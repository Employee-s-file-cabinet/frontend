import { MainField } from '../../components/UI/Fields/MainField';

const meta = {
  title: 'UI/MainField',
  component: MainField,
  tags: ['autodocs'],
};

export default meta;

export const Primary = {
  args: {
    theme: 'is-primary',
    size: 'is-normal',
    type: 'email',
  },
};
