export const LARGE_SCREEN = 1280
export const MID_SCREEN = 768

export const RegExPassword = "^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9]).{8,}$"
export const PasswordError = "Password must be at least 8 characters including a lowercase letter, an uppercase letter, and a number."
